# Deprecated path — do not write reports here

Canonical Research Scout reports live at:

`outputs/reports/research-scout/`

(see `tools/research/scout/config.py` → `REPORTS_DIR`)

This directory is kept only as a tombstone so old tooling/docs that still look under
`tools/research/reports/` hit this note instead of treating an empty (or stale)
folder as the source of truth.
